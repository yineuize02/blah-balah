server:
  port: 8885
spring:
  application:
    name: blah-store
  profiles:
    active: dev,common


mybatis-plus:
  mapper-locations:
    - classpath:mapper/*.xml
    - classpath:mapper_extend/*.xml

