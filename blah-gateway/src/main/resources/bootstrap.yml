server:
  port: 8801
spring:
  application:
    name: blah-gateway
  profiles:
    active: dev,common



#  cloud:
#    nacos:
#      discovery:
#        server-addr: ${NACOS_ADDR}
#      config:
#        server-addr: ${NACOS_ADDR}
#        file-extension: yaml

#logging:
#  level:
#    root: debug